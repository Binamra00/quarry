<?xml version="1.0"?>
<ruleset name="Smell-Ranker-Targeted"
    xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0 https://pmd.sourceforge.io/ruleset_2_0_0.xsd">

    <description>
        Targeted ruleset for Smell-Ranker.
        Uses NCSS (Non-Commenting Source Statements) for length metrics to ensure PMD 7 compatibility.
    </description>

    <!-- 1. God Class (The Big One) -->
    <rule ref="category/java/design.xml/GodClass" />

    <!-- 2. Long Method -> NCSS Count (Lines of Code) -->
    <!-- Replaces ExcessiveMethodLength which caused errors -->
    <rule ref="category/java/design.xml/NcssCount">
        <properties>
            <!-- Report methods longer than 50 lines (Standard heuristic) -->
            <property name="methodReportLevel" value="50" />
            <!-- Report classes longer than 500 lines -->
            <property name="classReportLevel" value="500" />
        </properties>
    </rule>

    <!-- 3. Complex Logic (Another proxy for Long Method) -->
    <rule ref="category/java/design.xml/CyclomaticComplexity" />

</ruleset>